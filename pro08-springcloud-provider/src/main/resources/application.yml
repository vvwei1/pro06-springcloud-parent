server:
  port: 1000
spring:
  application:
    name: atguigu-provider
eureka:
  client:
    service-url:
      defaultZone: http://localhost:5000/eureka/
#      配置被监控
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream